# web/modern — Svelte Frontend Source

How to read:

1. `src/boot.js` → `init.js` → `state.js` → `world.js` → `viewer.js`
2. `floors.js` / `interior.js` / `navigation.js` / `hud.js` — rendering
3. `live.js` / `route.js` / `visualization.js` — overlays

See `diagram.mmd`.
