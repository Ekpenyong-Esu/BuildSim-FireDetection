# pkg/graph — Routing

How to read:

1. `multifloor.go` — `BuildMultiFloorGraph()` merges per-floor graphs (imports `model`)
2. `dijkstra.go` — `Dijkstra()` shortest path (imports `model`)

See `diagram.mmd`.
