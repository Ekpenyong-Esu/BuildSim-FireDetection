# examples — API Usage Examples

How to read:

1. `api/` — curl examples per endpoint (`building`, `graph`, `equipment`, `room-layers`, `scenario`, `session`)
2. `go-client/main.go` — typed `pkg/client` usage

See `diagram.mmd`.
