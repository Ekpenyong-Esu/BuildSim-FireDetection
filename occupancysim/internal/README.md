# internal — Core Packages

How to read:

1. `sim/` — movement simulation (standalone)
2. `buildsim/` — BuildSim publisher (imports `sim`)
3. `api/` — HTTP surface (imports `sim`+`buildsim`)

See `diagram.mmd`.
